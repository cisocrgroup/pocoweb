[Unit]
Description=pocowebd the pocoweb rest daemon (%i)
Documentation=http://pocoweb.cis.lmu.de/documentation.php
Requires=

[Service]
Type=simple
PIDFile=${PCW_POCOWEB_PID_FILE}
ExecStart=${PCW_POCOWEB_EXE} ${PCW_POCOWEB_CONFIG}
Restart=true

[Install]
WantedBy=default.target
